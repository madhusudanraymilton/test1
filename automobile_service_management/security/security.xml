<odoo>
    <!-- Security Groups -->
    <record id="group_automobile_user" model="res.groups">
        <field name="name">Automobile User</field>
        <field name="category_id" ref="base.module_category_services"/>
    </record>

    <record id="group_automobile_manager" model="res.groups">
        <field name="name">Automobile Manager</field>
        <field name="implied_ids" eval="[(4, ref('automobile_service_management.group_automobile_user'))]"/>
        <field name="category_id" ref="base.module_category_services"/>
    </record>

    <!-- Optional: a simple rule example (if you need per-record restrictions later) -->
    <!--
    <record id="automobile_order_user_rule" model="ir.rule">
        <field name="name">Service orders: only own company</field>
        <field name="model_id" ref="model_automobile_service_order"/>
        <field name="domain_force">[('company_id','=',user.company_id.id)]</field>
        <field name="groups" eval="[(4, ref('automobile_service_management.group_automobile_user'))]"/>
    </record>
    -->

    <!-- OPTIONAL: Example menu access (you probably already have menu xml) -->
    <!--
    <record id="menu_automobile_root" model="ir.ui.menu">
        <field name="name">Automobile</field>
        <field name="sequence">30</field>
    </record>

    <record id="menu_automobile_orders" model="ir.ui.menu">
        <field name="name">Service Orders</field>
        <field name="parent_id" ref="menu_automobile_root"/>
        <field name="sequence">10</field>
        <field name="groups" eval="[(4, ref('automobile_service_management.group_automobile_user'))]"/>
    </record>
    -->
</odoo>
